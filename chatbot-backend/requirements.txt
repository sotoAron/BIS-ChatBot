# ── FastAPI & server ─────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
sse-starlette==2.1.3
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── HTTP client (Ollama API) ──────────────────────────────────────────────────
httpx==0.27.0

# ── Redis ─────────────────────────────────────────────────────────────────────
redis[hiredis]==5.0.7

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb==0.5.3

# ── Embeddings (CPU-only — $0 infraestructura) ───────────────────────────────
sentence-transformers==3.0.1
# torch y transformers se instalan vía --index-url CPU wheel en el Dockerfile

# ── PDF / DOCX / TXT parsing (Fase 3+4 — pymupdf para tablas MD) ─────────────
pymupdf4llm==0.0.17       # PyMuPDF + Markdown export (preserva tablas de fechas)
pymupdf==1.24.11          # dependencia de pymupdf4llm
pypdf==4.3.0              # mantenido como fallback para textos simples/DOCX
python-docx==1.1.2
langdetect==1.0.9

# ── Utilities ────────────────────────────────────────────────────────────────
numpy==1.26.4
python-multipart==0.0.9

pytest==8.2.2
pytest-asyncio==0.23.7
pytest-bdd==7.2.0
httpx==0.27.0              # AsyncClient para tests de endpoints
fakeredis[aioredis]==2.23.2  # Redis en memoria para tests unitarios
chromadb==0.5.3            # también en testing para InMemoryClient

pytest-mock==3.14.0
